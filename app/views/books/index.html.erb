<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'book',book:@book %>
    </div>
    <div class="offset-md-1">
    <div class="col-md-8">      
      <h2>Books</h2>
          <table>
             <thead>
             　 <tr>
                 <th>Title</th>
                 <th>Opinion</th>
              　</tr>
             </thead>
             <tbody>
              <!--eachはtBodyの中にいれる-->
               <% @books.each do |book| %>
               <tr>
                 <!--<td><!--%= user.image %></td>-->
                 <td><%= link_to book.title, book_path(book.id) %></td>
                 <td><%= book.body %></td>
               </tr>
                  <% end %>
          　　</tbody>
          </table>
    </div>
    </div>
  </div>
</div>
